{
    "institution": payload.quoteRequest.body.payee.partyIdInfo.partyIdentifier[0:6],
    "account": payload.quoteRequest.body.payee.partyIdInfo.partyIdentifier[6:],
    "amount": payload.prepare.body.amount.amount,
    "name": payload.quoteRequest.body.payee.personalInfo.complexName.firstName + payload.quoteRequest.body.payee.personalInfo.complexName.lastName
}